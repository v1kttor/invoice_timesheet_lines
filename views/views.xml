<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="invoice_time_sheet_lines.list">
      <field name="name">invoice_time_sheet_lines list</field>
      <field name="model">invoice_time_sheet_lines.invoice_time_sheet_lines</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="invoice_time_sheet_lines.action_window">
      <field name="name">invoice_time_sheet_lines window</field>
      <field name="res_model">invoice_time_sheet_lines.invoice_time_sheet_lines</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="invoice_time_sheet_lines.action_server">
      <field name="name">invoice_time_sheet_lines server</field>
      <field name="model_id" ref="model_invoice_time_sheet_lines_invoice_time_sheet_lines"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="invoice_time_sheet_lines" id="invoice_time_sheet_lines.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="invoice_time_sheet_lines.menu_1" parent="invoice_time_sheet_lines.menu_root"/>
    <menuitem name="Menu 2" id="invoice_time_sheet_lines.menu_2" parent="invoice_time_sheet_lines.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="invoice_time_sheet_lines.menu_1_list" parent="invoice_time_sheet_lines.menu_1"
              action="invoice_time_sheet_lines.action_window"/>
    <menuitem name="Server to list" id="invoice_time_sheet_lines" parent="invoice_time_sheet_lines.menu_2"
              action="invoice_time_sheet_lines.action_server"/>
    -->
  </data>
</odoo>